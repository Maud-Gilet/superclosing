<%= simple_form_for(company, url: company_create_nominal_path(company), method: :post, remote: true) do |f| %>
  <%= f.input :share_nominal_value, placeholder: "Nominal de l'action", input_html: { name: 'share_nominal_value' }, label: false %>

  <%= f.button :submit, "Enregistrer le nominal des actions" %>
<% end %>
